# {{ ansible_managed }}
# crictl : l'outil de diagnostic des conteneurs vus par le kubelet.
#   sudo crictl ps       sudo crictl pods       sudo crictl images
runtime-endpoint: unix:///run/containerd/containerd.sock
image-endpoint: unix:///run/containerd/containerd.sock
timeout: 10
